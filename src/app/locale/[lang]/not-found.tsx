import NotFoundPage from '../not-found';

export default function LangNotFound() {
  // For now, just render the main not-found page
  // In the future, this could have localized content
  return <NotFoundPage />;
} 